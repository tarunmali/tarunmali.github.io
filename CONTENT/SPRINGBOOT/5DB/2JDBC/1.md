```
<dependency>
    <groupId>mysql</groupId>
    <artifactId>mysql-connector-java</artifactId>
    <version>8.0.23</version>
</dependency>

<dependency>
    <groupId>org.postgresql</groupId>
    <artifactId>postgresql</artifactId>
    <version>42.2.23</version>
</dependency>
```

1. helps us to abstract the network calls
2. serialization of data
3. provides a common interface which can be implemented by various vendors
4. Open closed Princple
5. Extend my code to new database vendors
6. JDBC comes installed with java and its kind of an interface, comprises of so many interfaces
7. Interfaces
    1. Database driver (MYSQL DRIVER, POSTGRES DRIVER)
8. These drivers are coded against the common interface Driver

![image.png](/images/image-188.png)

![image.png](/images/image-189.png)

![image.png](/images/image-190.png)

![image.png](/images/image-191.png)

```
package com.example.one;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.CommandLineRunner;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class OneApplication
		implements CommandLineRunner
{
//	@Autowired
//	Apple apple;
	@Autowired
	ConnectDB connectDB;
	public static void main(String[] args) {
		SpringApplication.run(OneApplication.class, args);
	}
	@Override
	public void run(String... args) throws Exception {
//		apple.eatApple();
		connectDB.customConnect();
	}
}
```

```
package com.example.one;

import org.springframework.stereotype.Component;

import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.SQLException;

@Component
public class ConnectDB {
    void customConnect() throws SQLException {
        Connection conn= DriverManager.getConnection("jdbc:postgresql://localhost:5432/one_database", "tarunmac", "password");
//        However, manually using DriverManager.getConnection(...) is bad practice in Spring Boot for two reasons:
//
//        No Connection Pooling: DriverManager opens a new physical connection every single time the method is called, which is very slow. Spring Boot comes with HikariCP (a high-performance connection pool) by default.


    }
}

```

```
  Connection conn= DriverManager.getConnection("jdbc:postgresql://localhost:5432/one_database", "tarunmac", "password");
```

```
Connection conn= 
DriverManager.getConnection("
    jdbc:mysql://localhost:3306/first_demo", 
    "root(user)", 
    "password(password)"
);
```

```
public static Connection getConnection(String url, String user, String password) throws SQLException{
    
}
```

![image.png](/images/image-194.png)

![image.png](/images/image-195.png)

registeredDrivers maintains a list of mysql, postgres, mongo etc drivers

![image.png](/images/image-196.png)

# Writing production level code with try catch

```
  Connection conn= DriverManager.getConnection("jdbc:postgresql://localhost:5432/one_database", "tarunmac", "password");
```







