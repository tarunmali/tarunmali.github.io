---
title: 1
---

{% raw %}
present in ppa

![image.png](/DTRANSFER/notebook_images/DTRANSFER_ADCLCHJC_2DataStructures_3NAryTree_1__att_0001.png)

```python
    vector<vector<int>> levelOrder(NAryNode* root) {
        vector<vector<int>> result; // Result list to store levels
        // ToDo: Write Your Code Here.
        return result; // Return the level order traversal
    }
```

```python
class NAryNode {
public:
    int val; 
    vector<NAryNode*> children; 

    NAryNode() {
        children = vector<NAryNode*>(); 
    }

    NAryNode(int _val) {
        val = _val;
        children = vector<NAryNode*>(); 
    }

    NAryNode(int _val, vector<NAryNode*> _children) {
        val = _val;
        children = _children; 
    }
};
```

https://leetcode.com/problems/n-ary-tree-level-order-traversal/description/

```python
/*
// Definition for a Node.
class Node {
public:
    int val;
    vector<Node*> children;

    Node() {}

    Node(int _val) {
        val = _val;
    }

    Node(int _val, vector<Node*> _children) {
        val = _val;
        children = _children;
    }
};
*/

class Solution {
    public:
        vector<vector<int>> levelOrder(Node* root) {
            queue<Node*> q;
            vector<vector<int>> ans;
            if(!root) return ans;
            q.push(root);
            while(q.size()){
                int sz=q.size();
                vector<int> tmp;
                while(sz--){
                    Node* n=q.front();
                    q.pop();
                    tmp.push_back(n->val);
                    for(auto node: n->children){
                        q.push(node);
                    }
                }
                ans.push_back(tmp);
            }
            return ans;
        }
    };
```
{% endraw %}
