---
title: 1
---

{% raw %}
# ArrayList Init

```python
import java.util.Collections;
```

```python
Collections.swap(l,0,1);
Collections.replaceAll(l,"Mali","Pal");
Collections.reverse(l);
Collections.shuffle(l);//every time different
Collections.rotate(l,1);
```

```python
Collections.binarySearch(l,k);
//returns the index of the element in the list
//if the element is not present in the list it returns the 
//negative of the index where the element should be present
```

# Linear Search    
uses a different syntax

```python
l.countains(k);
l.indexOf(k); 
//if not found return -1
```

```python
Integer[] arr2 = {1, 2, 3, 4, 5};
List<Integer> l = Arrays.asList(arr2);
System.out.println(l.contains(3));
System.out.println(l.indexOf(3));
System.out.println(l.indexOf(6));
```

```text
true
2
-1
```

```python
Collections.min()
```
{% endraw %}
