[LeetCode - The World's Leading Online Programming Learning Platform](https://leetcode.com/problems/best-time-to-buy-and-sell-stock-ii/submissions/733385712/)

```cpp
int maxProfit(vector<int>& v) 
{
    int ans=0;
    for(long long i=1;i<v.size();i++)
    {
        if(v[i]>v[i-1])
        {
            ans+=v[i]-v[i-1];    
        }
    }
    return ans;
}
```
